project(MCDOC)

# file(GLOB SOURCES ${MC_SOURCE_DIR}/*.cpp ${MC_SOURCE_DIR}/*.hpp ${MC_SOURCE_DIR}/*.tpp)
# foreach(f ${SOURCES})
#   get_filename_component(basename ${f} NAME_WE)
#   get_filename_component(dir ${f} DIRECTORY)
#   add_executable(${basename} ${f})
# endforeach()
